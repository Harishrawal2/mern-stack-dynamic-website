<nav class="navbar border-bottom navbar-expand-lg py-2 navbar-dark bg-dark">
  <div class="container">
    <a class="navbar-brand" href="/">
    <img src="{{data.brandIconUrl}}" alt="" width="60" height="40" style="border-radius:20%">
     {{data.brandName}}</a>
    <button
      class="navbar-toggler"
      type="button"
      data-bs-toggle="collapse"
      data-bs-target="#navbarSupportedContent"
      aria-controls="navbarSupportedContent"
      aria-expanded="false"
      aria-label="Toggle navigation"
    >
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
        {{#each data.links}}
        <li class="nav-item">
          <a class="nav-link active" aria-current="page" href="{{this.url}}">
            {{this.label}}</a>
        </li>
       {{/each}}
        <button class="btn btn-outline-danger me-2">Sign in </button>
        <button class="btn btn-outline-success">Sign Up </button>

    </div>
  </div>
</nav>